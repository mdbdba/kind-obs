apiVersion: v1
kind: ConfigMap
metadata:
  name: edge-telemetry-collector-config
  namespace: telemetry
data:
  collector-config.yaml: |
    receivers:
      filelog:
        include:
          # Target the Grafana pods in the monitoring namespace
          - /var/log/pods/**/*.log
        exclude:
          - '*.gz'
        start_at: beginning
        include_file_path: true
        include_file_name: true
        operators:
        # Extract Kubernetes metadata from the path
        - type: regex_parser
          regex: '^/var/log/pods/(?P<namespace>[^_]+)_(?P<pod_name>[^_]+)_(?P<pod_uid>[^/]+)/(?P<container_name>[^/]+)/.*$'
          parse_from: attributes["log.file.path"]
          parse_to: attributes
        
        # Parse the Kubernetes log format in the Body field
        - type: regex_parser
          regex: '^(?P<timestamp>[^\s]+)\s+(?P<stream>stdout|stderr)\s+(?P<flag>\w)\s+(?P<content>.*)$'
          parse_from: body
          parse_to: attributes
        
        # Move the content to a message field 
        - type: move
          from: attributes.content
          to: body
        

    processors:
      batch:
        timeout: 1s
        send_batch_size: 1024
      resource:
        attributes:
          - key: app
            value: grafana
            action: upsert
          - key: k8s.namespace.name
            from_attribute: namespace
            action: upsert
          - key: k8s.pod.name
            from_attribute: pod_name
            action: upsert
          - key: k8s.container.name
            from_attribute: container_name
            action: upsert
          - key: topic
            value: grafana-logs
            action: upsert
    
    exporters:
      # debug:
      #   verbosity: detailed
      otlp:
        # Send to the bufstream service in queuing namespace
        endpoint: kafka-kafka-brokers.queuing.svc:9092
        tls:
          insecure: true
        headers:
          # Adding topic in headers for bufstream
          "X-Topic": "grafana-logs"
    
    
    service:
      pipelines:
        logs:
          receivers: [filelog]
          processors: [batch, resource]
          exporters: [otlp]

---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: edge-telemetry-collector
  namespace: telemetry
spec:
  selector:
    matchLabels:
      app: edge-telemetry-collector
  template:
    metadata:
      labels:
        app: edge-telemetry-collector
    spec:
      serviceAccountName: edge-telemetry-collector
      containers:
        - name: collector
          image: otel/opentelemetry-collector-contrib:latest
          args:
            - --config=/conf/collector-config.yaml
          volumeMounts:
            - name: config-volume
              mountPath: /conf
            - name: varlogpods
              mountPath: /var/log/pods
              readOnly: true
          env:
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
      volumes:
        - name: config-volume
          configMap:
            name: edge-telemetry-collector-config
        - name: varlogpods
          hostPath:
            path: /var/log/pods
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: edge-telemetry-collector
  namespace: telemetry
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: edge-telemetry-collector
rules:
  - apiGroups: [""]
    resources: ["pods", "namespaces"]
    verbs: ["get", "list", "watch"]
---
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: edge-telemetry-collector
subjects:
  - kind: ServiceAccount
    name: edge-telemetry-collector
    namespace: telemetry
roleRef:
  kind: ClusterRole
  name: edge-telemetry-collector
  apiGroup: rbac.authorization.k8s.io